/*Does the number's square form ends with itself*/

class Solution {
  static String autoMorphic(int number) {
    return (number * number + "").endsWith(number + "") ? "Automorphic" : "Not!!";
  }
}
